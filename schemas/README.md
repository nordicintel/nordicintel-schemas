# Schemas

Place shared JSON Schema definitions here as contracts are agreed.
Use descriptive `*.schema.json` filenames. Do not add placeholder schemas that
accept arbitrary documents: absence of a contract should remain explicit.
