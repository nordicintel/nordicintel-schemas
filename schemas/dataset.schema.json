{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "NordicIntel dataset metadata",
  "description": "Standardized harvester, scraper and wrapper metadata output for one language. Provider and dataset codes identify the provider's dataset; public identifiers are assigned by consuming systems.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "provider_code",
    "dataset_code",
    "language",
    "label",
    "dimension_ids",
    "dimension"
  ],
  "properties": {
    "provider_code": {
      "type": "string",
      "description": "Stable provider code, e.g. scb.",
      "pattern": "^[a-z][a-z0-9]*(?:_[a-z0-9]+)*$",
      "examples": [
        "scb",
        "kolada"
      ]
    },
    "dataset_code": {
      "type": "string",
      "description": "Opaque, case-sensitive provider dataset code, unique within its provider and shared across language versions. Do not slugify.",
      "pattern": "\\S",
      "examples": [
        "TAB335",
        "ABAKD12.px",
        "N11730_OU"
      ]
    },
    "language": {
      "$ref": "#/$defs/language_code",
      "description": "Language of this metadata document; no silent language fallback.",
      "examples": [
        "sv",
        "en"
      ]
    },
    "label": {
      "type": "string",
      "description": "Human-readable dataset name/title, as given by the provider.",
      "pattern": "\\S",
      "examples": [
        "Befolkning efter region och år"
      ]
    },
    "source": {
      "type": [
        "string",
        "null"
      ],
      "default": null,
      "description": "Dataset attribution as reported by the provider. This names the attributed source, which may differ from the provider. Resource URLs belong in the named URL fields or additional_urls. Omit or use null when unknown.",
      "examples": [
        "Statistiska centralbyrån",
        null
      ]
    },
    "description": {
      "type": [
        "string",
        "null"
      ],
      "default": null,
      "description": "Longer free-text description of the dataset.",
      "examples": [
        "Folkmängd vid årets slut."
      ]
    },
    "discontinued": {
      "type": [
        "boolean",
        "null"
      ],
      "default": null,
      "description": "Whether publication or updates for this Dataset have stopped. False means explicitly active; omission or null means unknown.",
      "examples": [
        true,
        false,
        null
      ]
    },
    "updated": {
      "$ref": "#/$defs/dateOrDatetime",
      "default": null,
      "description": "Provider-reported last Dataset modification, including observation, structural or metadata changes. Preserve a value from which the consumer can extract a valid calendar date. Timezone information and a standardized timestamp format are not required. This is not harvest time.",
      "examples": [
        "2026-09-15",
        "2026-09-15 09:00:00",
        "2026-09-15T09:00:00+02:00",
        null
      ]
    },
    "official_statistics": {
      "type": [
        "boolean",
        "null"
      ],
      "default": null,
      "description": "Provider-reported official national statistics status. Preserve true, false and unknown (omitted or null); never infer it solely from provider identity.",
      "examples": [
        true,
        false,
        null
      ]
    },
    "time_unit": {
      "type": [
        "string",
        "null"
      ],
      "enum": [
        "annual",
        "semiannual",
        "quarterly",
        "monthly",
        "weekly",
        "daily",
        "other",
        null
      ],
      "default": null,
      "description": "Granularity of statistical time periods, not publication frequency. Use other for a known granularity outside the enum; omit or use null when unknown.",
      "examples": [
        "annual",
        "quarterly",
        "other",
        null
      ]
    },
    "first_period": {
      "type": [
        "string",
        "null"
      ],
      "default": null,
      "description": "First period covered by the Dataset, either reported by the Provider or inferred by a local implementation; not necessarily a category label. Preserve the Provider's original form instead of standardizing it.",
      "pattern": "\\S",
      "examples": [
        "2020",
        "2020K1",
        null
      ]
    },
    "last_period": {
      "type": [
        "string",
        "null"
      ],
      "default": null,
      "description": "Last period covered by the Dataset, either reported by the Provider or inferred by a local implementation; not necessarily a category label. Preserve the Provider's original form instead of standardizing it.",
      "pattern": "\\S",
      "examples": [
        "2025",
        "2025K4",
        null
      ]
    },
    "next_release": {
      "$ref": "#/$defs/dateOrDatetime",
      "default": null,
      "description": "Date/datetime of the next expected release of the dataset, as announced by the provider.",
      "examples": [
        "2027-02-20",
        null
      ]
    },
    "notes": {
      "type": [
        "array",
        "null"
      ],
      "items": {
        "type": "string"
      },
      "default": null,
      "description": "Ordered dataset-level explanatory notes in the document language. Dimension and category notes belong at their corresponding levels.",
      "examples": [
        [
          "Avser årets slut."
        ]
      ]
    },
    "subject": {
      "type": "object",
      "properties": {
        "code": {
          "type": "string",
          "pattern": "\\S",
          "description": "Original subject code."
        },
        "label": {
          "type": "string",
          "pattern": "\\S",
          "description": "Subject name in this document language."
        }
      },
      "description": "Primary thematic classification with code, localized label or both. Omit when unavailable; null and empty objects are not accepted.",
      "additionalProperties": false,
      "anyOf": [
        {
          "required": [
            "code"
          ]
        },
        {
          "required": [
            "label"
          ]
        }
      ],
      "examples": [
        {
          "code": "BE",
          "label": "Befolkning"
        }
      ]
    },
    "role": {
      "type": "object",
      "properties": {
        "time": {
          "$ref": "#/$defs/stringArray",
          "uniqueItems": true,
          "description": "Existing dimension codes assigned the time role; references and cross-role uniqueness are application checks."
        },
        "geo": {
          "$ref": "#/$defs/stringArray",
          "uniqueItems": true,
          "description": "Existing dimension codes assigned the geo role; references and cross-role uniqueness are application checks."
        },
        "metric": {
          "$ref": "#/$defs/stringArray",
          "uniqueItems": true,
          "description": "Existing dimension codes assigned the metric role; references and cross-role uniqueness are application checks."
        }
      },
      "description": "Semantic roles assigned to existing dimension codes. Each supplied array is nonempty and unique; array order need not follow dimension order. Applications enforce at most one role per dimension.",
      "additionalProperties": false,
      "examples": [
        {
          "time": [
            "Time"
          ],
          "geo": [
            "Region"
          ]
        }
      ]
    },
    "dimension_ids": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/nonblank"
      },
      "description": "Complete ordered dimension codes. Array order establishes statistical dimension order, not object-key order. Applications check exact correspondence with dimension keys.",
      "minItems": 1,
      "uniqueItems": true,
      "examples": [
        [
          "Region",
          "Time"
        ]
      ]
    },
    "dimension": {
      "type": "object",
      "description": "JSON-stat2 dimension objects keyed by provider dimension code, defined locally in $defs. dimension_ids supplies dimension order.",
      "additionalProperties": {
        "$ref": "#/$defs/jstat_dimension"
      },
      "minProperties": 1,
      "propertyNames": {
        "$ref": "#/$defs/nonblank"
      }
    },
    "source_url": {
      "anyOf": [
        {
          "$ref": "#/$defs/url"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Provider's human-readable page for this dataset. The field names the original dataset page, not the attribution in source."
    },
    "doc_url": {
      "anyOf": [
        {
          "$ref": "#/$defs/url"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Provider's primary documentation or methodology URL for this dataset."
    },
    "metadata_url": {
      "anyOf": [
        {
          "$ref": "#/$defs/url"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Provider's endpoint or file containing metadata for this dataset."
    },
    "data_url": {
      "anyOf": [
        {
          "$ref": "#/$defs/url"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Provider's endpoint or file for this dataset's observations. Request bodies and execution settings belong to the retrieval implementation."
    },
    "additional_urls": {
      "$ref": "#/$defs/additional_urls",
      "description": "Optional dataset-wide resources that do not fit the named URL fields. Never replace or duplicate source_url, doc_url, metadata_url or data_url here. Category-specific resources belong in the relevant dimension extension."
    },
    "extension": {
      "type": "object",
      "additionalProperties": true,
      "description": "Additional provider metadata for the whole Dataset, including scalar values, arrays and nested objects. ONLY use this for information that cannot be parsed or mapped into the defined Dataset fields. Do not duplicate those fields, introduce aliases for them, or put URLs here that belong in the named URL fields or additional_urls.",
      "examples": [
        {
          "revision_policy": "Preliminary figures may be revised."
        }
      ]
    },
    "paths": {
      "type": [
        "array",
        "null"
      ],
      "items": {
        "$ref": "#/$defs/path_array"
      },
      "default": null,
      "description": "Ordered thematic chains from broader to narrower nodes. Multiple chains describe multiple thematic memberships, never API routes or dataset lookup keys.",
      "examples": [
        [
          [
            {
              "id": "population",
              "label": "Befolkning"
            },
            {
              "id": "count",
              "label": "Folkmängd"
            }
          ]
        ]
      ]
    },
    "contacts": {
      "type": [
        "array",
        "null"
      ],
      "items": {
        "$ref": "#/$defs/contact"
      },
      "default": null,
      "description": "Provider-reported contacts. Populate the named attributes whenever the supplied information can be parsed or mapped to them. Additional properties are ONLY for information that cannot be represented by those attributes.",
      "examples": [
        [
          {
            "organization": "Statistikmyndigheten",
            "email": "statistics@example.org"
          }
        ]
      ]
    }
  },
  "$defs": {
    "stringArray": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/nonblank"
      },
      "description": "Nonempty ordered list of nonblank strings; the containing property defines whether these are codes or text.",
      "minItems": 1
    },
    "path_node": {
      "type": "object",
      "required": [
        "id",
        "label"
      ],
      "properties": {
        "id": {
          "type": "string",
          "pattern": "\\S",
          "description": "Original thematic node identifier, not a dataset access key."
        },
        "label": {
          "type": "string",
          "pattern": "\\S",
          "description": "Thematic node label in the document language."
        },
        "url": {
          "$ref": "#/$defs/url",
          "description": "Optional classification or documentation URL for this thematic node."
        }
      },
      "description": "One thematic node identified by the provider's thematic node code and localized label, with an optional supporting URL.",
      "additionalProperties": false
    },
    "path_array": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/path_node"
      },
      "description": "A nonempty thematic chain, ordered from broad to narrow classification.",
      "minItems": 1
    },
    "dateOrDatetime": {
      "type": [
        "string",
        "null"
      ],
      "pattern": "\\S",
      "description": "Provider date or timestamp text from which a valid calendar date can be extracted, or null when unknown. Preserve provider formatting; timezone information is optional. Date extractability is a consumer responsibility, not enforced by a format or timestamp pattern.",
      "examples": [
        "2026-09-15",
        "2026-09-15T09:00:00",
        "2026-09-15 09:00:00",
        null
      ]
    },
    "contact": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "pattern": "\\S",
          "description": "Contact person or team name."
        },
        "email": {
          "type": "string",
          "format": "email",
          "pattern": "\\S",
          "description": "Email address, not a mailto URL; enable email format checking."
        },
        "phone": {
          "type": "string",
          "pattern": "\\S",
          "description": "Telephone number as text, preserving prefixes and extensions."
        },
        "organization": {
          "type": "string",
          "pattern": "\\S",
          "description": "Organization responsible for this contact."
        },
        "address": {
          "type": "string",
          "pattern": "\\S",
          "description": "Postal or visiting address as provider text."
        },
        "url": {
          "$ref": "#/$defs/url",
          "description": "Contact page URL."
        }
      },
      "description": "Provider-reported contact. Additional properties are ONLY for information that cannot be parsed or mapped into name, email, phone, organization, address or url. Do not use alternate keys or raw copies to bypass or duplicate these attributes. At least one property must be supplied.",
      "additionalProperties": true,
      "minProperties": 1
    },
    "jstat_dimension": {
      "type": "object",
      "required": [
        "category"
      ],
      "properties": {
        "label": {
          "type": "string",
          "pattern": "\\S",
          "description": "Provider's dimension label in this document language; omit if unavailable."
        },
        "category": {
          "$ref": "#/$defs/jstat_category",
          "description": "Indexed categories and localized labels, with optional notes and units by category."
        },
        "note": {
          "$ref": "#/$defs/stringArray",
          "description": "Ordered explanatory notes applying to the whole dimension."
        },
        "extension": {
          "type": "object",
          "properties": {
            "elimination": {
              "type": "boolean",
              "default": false,
              "description": "Whether this dimension may be omitted from a data selection. Construction default is false. True does not require an elimination_value; validation inserts neither default.",
              "examples": [
                false,
                true
              ]
            },
            "elimination_value": {
              "type": [
                "string",
                "null"
              ],
              "pattern": "\\S",
              "default": null,
              "description": "Optional category code to use when this dimension is omitted. Null when unspecified. A supplied code must identify a category in this dimension, including a sole category identified by its label map; consumers check this relationship.",
              "examples": [
                null,
                "00"
              ]
            },
            "additional_urls": {
              "$ref": "#/$defs/additional_urls",
              "description": "Additional dimension resources. Set category_id only for a resource specific to an existing category of this dimension. Do not replace or duplicate the Dataset's named URL fields."
            }
          },
          "additionalProperties": true,
          "description": "Harvester metadata and additional provider details. Use defined attributes whenever applicable; preserve other metadata without duplicating them."
        },
        "source": {
          "type": "string",
          "description": "Attribution for this dimension as reported by the provider, which may name a different originating organization."
        },
        "updated": {
          "$ref": "#/$defs/dateOrDatetime",
          "description": "Provider-reported dimension update date or timestamp with the same permissive handling as Dataset.updated."
        },
        "class": {
          "const": "dimension",
          "description": "Optional JSON-stat2 class marker."
        }
      },
      "description": "JSON-stat2 dimension metadata, keyed by dimension code in the enclosing map. Harvester-specific metadata belongs in extension. URL resources use extension.additional_urls.",
      "additionalProperties": false
    },
    "jstat_category": {
      "type": "object",
      "description": "JSON-stat2 categories. Supply an index, or a single category label when the dimension has exactly one category. Labels are optional when an index is supplied.",
      "properties": {
        "index": {
          "description": "Category ordering as an ordered code array or a code-to-position object.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "$ref": "#/$defs/nonblank"
              },
              "minItems": 1,
              "uniqueItems": true
            },
            {
              "type": "object",
              "minProperties": 1,
              "description": "Category code to zero-based position; positions must be unique and contiguous.",
              "additionalProperties": {
                "type": "integer",
                "minimum": 0
              },
              "propertyNames": {
                "$ref": "#/$defs/nonblank"
              },
              "examples": [
                {
                  "00": 0,
                  "01": 1
                }
              ]
            }
          ],
          "examples": [
            [
              "00",
              "01"
            ],
            {
              "00": 0,
              "01": 1
            }
          ]
        },
        "label": {
          "type": "object",
          "description": "Provider labels keyed by existing category code. Omit unavailable labels; do not invent translations. With no index, exactly one category label identifies the sole category.",
          "minProperties": 1,
          "additionalProperties": {
            "$ref": "#/$defs/nonblank"
          },
          "propertyNames": {
            "$ref": "#/$defs/nonblank"
          },
          "examples": [
            {
              "00": "Riket",
              "01": "Stockholms län"
            }
          ]
        },
        "note": {
          "type": "object",
          "minProperties": 1,
          "additionalProperties": {
            "$ref": "#/$defs/stringArray"
          },
          "description": "Category code to ordered notes; each key must reference an existing category.",
          "propertyNames": {
            "$ref": "#/$defs/nonblank"
          }
        },
        "unit": {
          "type": "object",
          "additionalProperties": {
            "type": "object",
            "properties": {
              "label": {
                "type": "string",
                "minLength": 1,
                "pattern": "\\S",
                "description": "Provider's unit label in the document language."
              },
              "decimals": {
                "type": "integer",
                "minimum": 0,
                "description": "Nonnegative decimal places for display; not a statement of measurement accuracy."
              },
              "position": {
                "type": "string",
                "enum": [
                  "start",
                  "end"
                ],
                "description": "Placement of the unit symbol relative to the value: start or end."
              },
              "symbol": {
                "type": "string",
                "description": "Unit symbol, such as %, EUR or kr."
              }
            },
            "additionalProperties": true,
            "minProperties": 1,
            "description": "Unit metadata with optional label, decimals, symbol and position; additional provider properties are allowed. Preserve known unit information even when precision is unavailable. Omit unknown decimals rather than inventing a value.",
            "examples": [
              {
                "label": "antal",
                "decimals": 0,
                "position": "end"
              }
            ]
          },
          "propertyNames": {
            "$ref": "#/$defs/nonblank"
          },
          "description": "Units by existing category code, commonly on a metric dimension. Omit unknown units."
        },
        "extension": {
          "type": "object",
          "description": "Additional provider category metadata. Use the defined category fields whenever possible. Category-specific resources belong in the enclosing dimension's extension.additional_urls with category_id.",
          "additionalProperties": true,
          "examples": [
            {
              "provider_details": {
                "flags": [
                  "a"
                ],
                "value": null
              }
            }
          ]
        },
        "child": {
          "type": "object",
          "additionalProperties": {
            "type": "array",
            "items": {
              "$ref": "#/$defs/nonblank"
            },
            "uniqueItems": true
          },
          "description": "Category hierarchy: parent category code to its immediate child category codes. All codes refer to categories in this dimension."
        },
        "coordinates": {
          "type": "object",
          "additionalProperties": {
            "type": "array",
            "items": {
              "type": "number"
            },
            "minItems": 2,
            "maxItems": 2
          },
          "description": "Geographic category code to [longitude, latitude]."
        }
      },
      "additionalProperties": false,
      "anyOf": [
        {
          "required": [
            "index"
          ]
        },
        {
          "required": [
            "label"
          ],
          "properties": {
            "label": {
              "minProperties": 1,
              "maxProperties": 1
            }
          }
        }
      ]
    },
    "nonblank": {
      "type": "string",
      "pattern": "\\S",
      "description": "At least one non-whitespace character; validation does not trim or normalize text."
    },
    "language_code": {
      "type": "string",
      "enum": [
        "sv",
        "en"
      ],
      "description": "Supported metadata languages: Swedish (sv) and English (en)."
    },
    "url": {
      "type": "string",
      "format": "uri",
      "pattern": "^[Hh][Tt][Tt][Pp][Ss]?://[^\\s/?#]+[^\\s]*$",
      "description": "Absolute HTTP(S) resource URL. Resolve relative provider URLs against their original page or endpoint."
    },
    "additional_url": {
      "type": "object",
      "required": [
        "url",
        "label"
      ],
      "properties": {
        "url": {
          "$ref": "#/$defs/url"
        },
        "label": {
          "$ref": "#/$defs/nonblank",
          "description": "Human-readable resource label."
        },
        "description": {
          "type": "string",
          "description": "Optional explanation of the resource or its use."
        },
        "category_id": {
          "$ref": "#/$defs/nonblank",
          "description": "Use only inside a dimension's extension.additional_urls. Identifies an existing category in that dimension; omit for dimension-wide resources."
        }
      },
      "additionalProperties": true,
      "description": "An additional resource with required URL and label. Preserve useful extra provider metadata as additional properties. Must not replace or duplicate a named dataset URL field."
    },
    "additional_urls": {
      "type": [
        "array",
        "null"
      ],
      "items": {
        "$ref": "#/$defs/additional_url"
      },
      "description": "Additional resources, or null when unavailable. Omit when none exist."
    }
  },
  "$comment": "Defaults are annotations, not inserted values. Consumers check dimension membership, category positions and references, dimension roles, extension elimination values, additional URL scope and nonduplication, and date extractability. See ../DATASET-MODEL.md.",
  "$id": "https://raw.githubusercontent.com/nordicintel/nordicintel-schemas/v2.0.0/schemas/dataset.schema.json"
}
