# Examples

Add small JSON examples alongside each agreed contract. Document which schema
each example targets. Add deliberately invalid cases when validation tooling
is introduced, keeping them clearly separate from valid examples.

Use synthetic or public information only; never include credentials.
